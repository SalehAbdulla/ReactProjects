1) save the file under frontend directory
2) install react-router-dom react-toastify
       What is a "toast notification"?
            1) Message that pops up in the corner of the screen and disappears automatically after a few seconds.
3) Clear the directory
4) Add the assets folder
5) Install Tailwind -- Solve 3 tailwing projects
  missed step here  -- Wrap <App/> with BrowserRouter in main.tsx
6) Create 3 directories --> pages, context, components
          1) pages/ Create The following 9 Pages components
               1) Home
               2) Product
               3) PlaceOrder
               4) Login
               5) Contact
               6) Collection
               7) Cart
               8) About
               9) Orders
               10) Page not found
7) Remove StrictMode and Setup the BrowserRouter App in the main.jsx
8) Add the custom tailwind to the root App container px-4 sm:px-[5vw] md-[7vw] lg:px-[9vw]
9) Add import {Routes, Route} from 'react-router-dom' <Routes></Routes> 
10) Add the routes for all 9 pages --> <Route path="/" element={<Home/>}/>

------ Memorise it

11 ) Create a navBar component and mounts it above the router,
     By doing that we will have the navBar above all pages.

12 ) add tailwindcss style flex items-center justify-content py-5 font-medium

13 ) add the logo - w-36

14 ) add ul -> hidden sm:flex gap-5 text-sm text-gray-700

import { NavLink } from "react-router-dom";
15 ) add <NavLink className=""/>

16 ) add 5 NavLink -> HOME COLLECTION ABOUT Contact
        <NavLink to="/contact" className="flex flex-col items-center gap-1">
          <p>CONTACT</p>
          <hr className="w-2/4 border-none h-[1.5px] bg-gray-700 hidden" />
        </NavLink>
17 ) for <hr/> effect --> add a.active hr {display: block};

------ Memorise it
